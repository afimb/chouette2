<%= form.input :references_type, :as => :select, :include_blank => t("import_tasks.new.all"), :input_html => { :title => t("formtastic.titles.import_task.references_type") } %>
<%= form.input :object_id_prefix, :input_html => { :value => @referential.prefix } %>
<%= form.input :coordinate_projection, :as => :select, :include_blank => false, :collection => RegtoppImport.available_srids, :selected  => "EPSG:"+@referential.projection_type %>
<%= form.input :version, :as => :select, :collection => RegtoppImport.available_versions%>
<%= form.input :charset_encoding, :as => :select, :collection => RegtoppImport.charset_encoding%>
<%= form.input :calendar_strategy, :as => :select, :collection => RegtoppImport.calendar_strategy%>
<%= form.input :batch_parse, :as => :boolean, :default: true%>
