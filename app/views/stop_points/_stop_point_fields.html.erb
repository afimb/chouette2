<%= form.inputs :class => 'nested-fields stop-point' do %>

    <%= p.input :id, :for => sp, :as => :check_boxes, :collection => [sp.id.to_s, sp.name], :input_html => (@journey_pattern.new_record? ? { :checked => 'checked' }:{})  %>
    <%= p.input :destination_display_id, :for => sp, :label => sp.name, :as => :search_destination_display, :json => referential_autocomplete_destination_displays_path(@referential, :format => :json), :hint_text => t('search_hint'), :no_result_text => t('no_result_text'),:searching_text => t('searching_term'), :tokenLimit => 1, :input_html => { :class => "destination_display_id", :"data-pre" => Rabl::Renderer.new('autocomplete_destination_displays/index', [sp.destination_display].compact, :view_path => 'app/views', :format => :json, :scope => self ).render } %>

<% end %>


