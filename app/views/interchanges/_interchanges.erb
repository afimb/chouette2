<div class="page_info">
  <span class="search"> <%= t("will_paginate.page_entries_info.search") %></span> <%= page_entries_info( @interchanges ) %>
</div>
<div class="interchanges paginated_content">
   <%= paginated_content(@interchanges) %>
</div>
<%= render 'shared/paginate', { datas: @interchanges } %>
